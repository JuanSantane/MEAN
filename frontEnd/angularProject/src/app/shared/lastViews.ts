export class LastViewsByDeviceType {
  mra: string;
  lsr: string;
  voc: string;

  constructor(mra: string, lsr: string, voc: string) {
    this.mra = mra;
    this.lsr = lsr;
    this.voc = voc;
  }
}
